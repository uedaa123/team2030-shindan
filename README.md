# チーム2030 プロジェクト診断 ／ 公開用サイト

**5画面**で、今週やるプロジェクトが1つ決まる。1人2〜3分。

やってみたいことは**3段階**で絞る。いきなり26分野から選ばせると粒度がばらばらで選べないため。

1. **大きく6つ**（自然と生きもの／人と暮らし／まちと場所／しごととお金／技術と仕組み／文化とものづくり）
2. **その中の分野**（選んだグループの分野だけ出す。3つまで）
3. **実際のプロジェクトを1つ選ぶ**（8件＋「どれもピンとこない」）

そのあと **勇誠義礼** と **ウェルスダイナミクス** を選ぶだけ（受講ずみなので判定も説明もしない）。

結果は**自分で選んだプロジェクト**が主役になる。

素のHTML/CSS/JS。ビルド不要、外部CDNなし、フレームワークなし、localStorage不使用（引継ぎ資料 §9）。

---

## ファイル構成

```
index.html                  公開用。data/cases.json を fetch する
config.js                   ★運営が触るのはここだけ（データの場所・記録先・質問数）
assets/style.css            見た目
assets/app.js               設問・スコアリング・結果・コピー・記録
data/cases.json             プロジェクト289件（26分野）＋もう動いている場所33件
build.mjs                   オフライン配布用の1ファイル版を吐く
dist/
  TEAM2030_プロジェクト診断_offline.html   ↑の出力。file:// で直接開ける
tools/
  make-cases.mjs            2本のxlsx → data/cases.json。分野の寄せ方・言い換えの判断が全部ここ
  verify-scoring.mjs        結果の出し方を変えたときの必須チェック
  gas-endpoint.gs           回答をスプレッドシートに貯めるGASスクリプト
```

---

## 動かす

### 手元で見る

`index.html` を **file:// で直接開くと動かない**。`data/cases.json` の fetch が CORS で落ちるため
（そう表示される）。手元の確認は次のどちらか。

ローカルサーバーを立てる：

```bash
npx serve .
```

または、データ埋め込み版を開く（ダブルクリックでよい）：

```
dist/TEAM2030_プロジェクト診断_offline.html
```

### 埋め込み版を作り直す

`assets/` か `data/` を直したら、必ず作り直す。中身は公開用と同じものが1枚に入るだけ。

```bash
node build.mjs
```

---

## 公開する

### いま公開されている場所

| | |
|---|---|
| 公開URL | **https://uedaa123.github.io/team2030-shindan/** |
| リポジトリ | https://github.com/uedaa123/team2030-shindan （**public**） |
| Claudeアーティファクト版 | https://claude.ai/code/artifact/bb37437a-0431-4cfa-8054-6eb2cbf3faf6 （非公開・Claudeアカウントが要る） |

公開URLはログイン不要で誰でも開ける。2026-09-14に植田さんの判断で公開した。

**リポジトリが public なので、`data/cases.json`（事例113件）も誰でも落とせる状態にある。**
引継ぎ資料 §10 が「113件の事例集と各種解釈は有料コンテンツの一部で、全公開は運営判断が要る」
と書いているのはこの状態のこと。運営の判断でこれを止めるときは、リポジトリを private にすると
Pages も止まる（無料プランの場合）ので、UTAGE か Netlify に移すことになる。

`robots.txt` と `<meta name="robots" content="noindex">` を置いてあるので、検索には出ない。
ただし**検索避けであって、アクセス制限ではない**。URLを知っていれば誰でも開ける。

### 更新のしかた

`main` に push すれば1〜2分で反映される。

```bash
node build.mjs
git add -A && git commit -m "..." && git push
```

**`node build.mjs` を先に流すこと。** 2つのことをやっている。

1. `dist/` の埋め込み版を作り直す
2. `index.html` の CSS/JS のURLに `?v=<中身のハッシュ>` を打ち直す

2がないと、GitHub Pages のキャッシュ（10分ほど）のせいで、
更新したのに古い画面が出たままになる。中身が変わったときだけ版が変わるので、
何度流しても差分は出ない。

### もう1つの選択肢：Netlify

リポジトリを公開したくない場合は、このフォルダごと
[app.netlify.com/drop](https://app.netlify.com/drop) にドラッグ＆ドロップ。
アカウント不要で数秒でURLが出る。**未ログインのサイトは一定期間で消えるので、
無料アカウントは作っておく**（固定URL・差し替え・削除ができるようになる）。
`_headers` はNetlify用の設定ファイルで、Pages では何もしない。

### 段階2：本番（UTAGEの会員エリア）

UTAGEにカスタムHTMLブロックがあれば `dist/...offline.html` の中身をそのまま貼れる（1ファイルなので
外部ファイルの参照がいらない）。なければ Netlify のURLを会員ページから iframe で呼ぶ。

**会員だけがアクセスできる状態にする（完了条件2）のは、このコードの外側の話。**
いまの GitHub Pages はURLを知っている人なら誰でも見られる。UTAGEの会員エリアに入れるか、
Netlifyのパスワード保護（有料プラン）を使うか、どちらかを運営と決めること。

---

## データの出どころ

2本のExcelをマージしてある。

| もと | 件数 | 特徴 |
|---|---|---|
| `TEAM2030_プロジェクト事例200ｖ２.xlsx` | 200件・26分野 | 分野の幅が広い（政治・経済／AI・デジタル／動物／旅・場づくり など） |
| `TEAM2030_プロジェクト90_大義つき.xlsx` | 90件・12分野 | **行政の入口**と**着地点**が書いてある |

**分野は200件側の26分野に寄せた。** 90件側の12分野をどこに寄せたかは
`tools/make-cases.mjs` の `G90` / `G90_ITEM` に1件ずつ書いてある。
90件側にしかない「行政の入口」「着地点」は捨てずに持ってきてある
（結果画面の「行政の入口」「そのあと」はこの90件側から出ている）。

合わせて289件。分野ごとの件数は `node tools/make-cases.mjs` が毎回出す。
**「動物」3件と「旅・場づくり」4件だけ薄い**。選ぶと3〜4件しか出ないので、
結果画面に「分野をもう1つ選ぶともっと出ます」と出るようにしてある。

---

## 運営がやること

### プロジェクトを足す・直す

`data/cases.json` の `items[]` に足すだけ。コードは触らなくてよい。
必要なキーは `id / title / genre / cause / reach / scale / scaleLabel / step`。
`land`（そのあと）/ `orgs`（行政の入口）/ `numbers`（測る数字）/ `ref`（元ネタ）は任意。
`genre` は `genres` にある名前のどれか。直したら `node build.mjs` で埋め込み版も作り直す。

元のExcelから作り直すときは
`node tools/make-cases.mjs <200件TSV> <90件TSV> <旧cases.json> <参照サイトJSON>`。

### 読みにくい言葉を直したいとき

`tools/make-cases.mjs` の `REWRITE` に **id → {title, summary, step}** で書けば上書きされる。
元の文は `titleSrc` / `summarySrc` / `stepSrc` に残るので、いつでも戻せる。

いま39件を直してある。直した理由は2つ。

- **むずかしい言葉**（属人化・TNR・ノーコード・ローリングストック・地域団体商標・
  オープンデータ・関係人口・ジビエ・RMO・ケアラーなど）
- **「どこに行って、誰に、何と言うのか」が書いていない一歩目**。
  例：「一番混乱している紙を1枚もらってくる」→
  「自治会かPTAの役員さんに『いま一番ややこしい表はどれですか』と聞いて、その紙を1枚もらう」

### 回答を記録する／結果をメールで送る

1. `tools/gas-endpoint.gs` の手順どおりGASをデプロイする
   （**「次のユーザーとして実行：自分」で出すこと。** メール送信に必要）
2. 出たURLを `config.js` の `logEndpoint` に貼る

**貼るまでは、入口の「お名前とメールアドレス」の設問ごと出ない**（送り先がないため）。
記録の送信も一切行わない。ツールは普通に動く。

### 名前とメールを入口で必須にしている理由

あとで任意に入れてもらう形にしていたが、入れなかった人は
シートに `start` / `result` が残っても**セッションIDだけ**で、誰なのか分からない。
それでは「始めたのに結果まで来ていない人に声をかける」という本来の目的が果たせない。

入口で取れば、その人が始めた時点で名前が残る。結果は最後に自動で送る
（「診断が終わったら、結果をこのアドレスにお送りします」と先に伝えてある）。

記録されるのは4種類。

| イベント | いつ | 使いみち |
|---|---|---|
| `start` | 最初の選択をタップした時 | 「開いたが結果まで到達していない人」＝脱落の予兆 |
| `result` | 結果画面が出た時 | 選んだ分野・タイプ・提示された候補 |
| `copy` | コピーボタンを押した時 | 次の行動まで進んだ人の数 |
| `mail` | メールを送った時 | **お名前とメールアドレスが残る** |

スプレッドシートの列：受信時刻／イベント／セッション／お名前／メールアドレス／
設問セット／選んだ分野／ウェルスダイナミクス／勇誠義礼／提示された候補。

GASのエディタで動かせる関数が2つある。

- `未到達のセッション()` … start だけで result が無い人を出す
- `名簿()` … 名前とメールアドレスの一覧（重複なし）

**メールの通数に上限がある。** 個人のGmailで1日100通、Workspaceで1500通。
1期20〜40名なら問題にならないが、上限に当たると**送信だけが静かに失敗する**。

### 質問数を切り替える

`config.js` の `questionSet` を `"short"` にすると2問版（分野・勇誠義礼）になる。
URLに `?q=2` を付けても同じ。既定は3問。

2問版ではウェルスダイナミクスを聞かないので、「あなたの入り方」は勇誠義礼だけの短い版になる。

---

## 開発者がやること

### 配点を変えたら

**必ずこれを流す。** 引継ぎ資料 §5 が要求している「4タイプ×3地域で結果が変わること」の確認。
`assets/app.js` の `score()` をそのまま呼んでいるので、配点を変えれば結果もここに出る。

```bash
node tools/verify-scoring.mjs
```

誰がやっても同じ5件が出るようになっていたら、非ゼロで落ちる。

### 分野を足すとき

1. `tools/make-cases.mjs` の `GENRE` / `GENRE_ORDER` に足す
2. `assets/style.css` に `--h11` / `--h11v`（文字用の濃い色と、塗り用の鮮やかな色）を足す
3. `assets/app.js` の `GENREDESC` に1行説明を足す
4. `node tools/make-cases.mjs ...` → `node build.mjs` → `node tools/verify-scoring.mjs`

**分野あたり最低6件は確保すること。** 選ばせたのに数件しか出ないのが、このツールで
いちばんやってはいけない失敗。`make-cases.mjs` が分野ごとの件数を出すので必ず見る。


---

## いまの状態

### できていること

- **5画面**・1問1画面・進捗バー・戻る
- **聞き方は3段階**（大きく6つ → 分野 → プロジェクト）。いきなり26分野を並べると
  粒度がばらばらで選べなかったため（2026-09-21 植田さんの指摘）
- 選択肢の「たとえば」は**プロジェクト名**を出す。一歩目だと役所の手続きの話が多く、
  分野とずれて見えた（観光なのに「職人を1人口説く」など）
- 「複数えらべます」を目立つバッジで出し、下に固定した「次へ」に選択数を常時表示
- 上の設問を変えたら、その下の答えは自動で捨てる（選べなくなった分野が残らない）
- 勇誠義礼もウェルスも**選ぶだけ**。受講ずみなので説明は出さない
- **結果は3つに分けて、最初に読み方を出す**
  1. **やってみるプロジェクト** … 自分で選んだ1つを大きく＋「今週やること」→
     「○○のあなたは、こう入る」を続けて出す。そのあと選ばなかった候補4件
  2. **進め方** … タイプ別の**汎用4ステップ**。どのプロジェクトでも使える。
     「人にどう動いてもらうか」ではなく、**自分ひとりで進めるときどうやると止まらないか**を書いている
  3. **共有する** … 診断結果を1本のテキストにしてコピー。バディにもチームにも同じ文を貼る。
     「話せますか？」のような問いかけは入れない（誘い方は貼る人が決める）
- プロジェクト289件・26分野。分野の並びは、幅があることが最初の画面で伝わるよう
  似たものを固めず散らしてある
- **元ネタは275/289件がリンクになっている**（参照サイトシートのURLと突き合わせ）。
  「チーム2030 …」「検索：…」の14件だけ文字のまま
- **説明のラベルを取り違えないようにしてある**。200件側の `forwho`（誰のためになるか）と
  90件側の `why`（大義）は別物なので、別のキーに分けて別のラベルで出している
  （以前は同じ「これで助かるのは」で出していて、意味の通らない行になっていた）
- スマホ幅375px前提。タップ領域は48px以上。ダークモード対応

### やめたこと（2026-09-20 植田さんの指示）

| やめたもの | 理由 |
|---|---|
| 住んでいるのはどんなところですか | 場所を聞かないと事例が出せない構造をやめた |
| その土地には何がありますか | 同上 |
| 気になっていることは何ですか | 分野の選択が同じ役割を果たす |
| ひと月に使える時間／最初に出せるお金 | プロジェクト選びと関係が薄い |
| どこまで届けたいですか（射程） | どちらでもよく、答えづらい。分類できない人も出る |
| 勇誠義礼の判定2問 | 受講ずみ。判定ではなく「タイプを選ぶだけ」に |
| 勇誠義礼・ウェルスの説明文 | 受講者はもう知っている |
| 結果の「お金の見通しチェックリスト」 | お金を聞かなくなったので |
| 「もう動いている場所」（実在事例33件）の表示 | そちらに行く人がTEAM2030から抜けてしまい、誰の得にもならないため。**データは `data/cases.json` の `reals` に残してある**ので、方針が変わればすぐ戻せる |
| バディを決める前提の文言 | バディはもう決まっている。「決めるとき」ではなく「もう決まっているなら頼む／違えばチームで探す」に |
| その土地には何がありますか | 同上 |
| 気になっていることは何ですか | 分野の選択が同じ役割を果たすので統合 |
| ひと月に使える時間 | プロジェクト選びと関係が薄い |
| 最初に出せるお金 | 同上 |
| 結果の「お金の見通しチェックリスト」 | 同上 |
| 旧データのビジョン80（80件） | 新しい89件が同じ役割で、大義・行政の入口・測る数字まで揃っている上位互換 |

副作用が1つ。お金と時間を聞かなくなったので「いまは少し届かないもの」の判定ができなくなった。
代わりに**規模Lのもの（十数人か許認可が要る）を「人が集まったら、これも」として出している**。
成長の行き先を残すという意図は引き継いである。

### 重いテーマの扱い（2026-09-20 植田さんの指示）

「TEAM2030は前向きで明るい未来のためにあるから、負の側面すぎるのはずれてる」。

- **落としたもの 1件**：自殺対策そのもののプロジェクト。前向きに言い換えると内容を偽ることになるため
- **大義を言い換えたもの 13件**／**名前を言い換えたもの 8件**。やることは変えていない
- 元の文は `causeSrc` / `titleSrc` / `genreSrc` にそのまま残してあるので、いつでも戻せる
- 判断はすべて `tools/make-cases.mjs` の上のほうの表（`LIGHTEN` / `RETITLE` / `DROP` / `GENRE`）に
  書いてある。**気に入らなければ表を直して流し直すだけ**

### 性格（ウェルスダイナミクス・勇誠義礼）の使いどころ

**順位には使っていない。** 89件に性格タグを機械で振ると当てずっぽうになるため
（旧データで実際に起きていた。「礼」が113件中56件に付いていた）。

性格は**「あなたの入り方」を書くほうにだけ使っている**。32通りを1本ずつ手で書いてある。
ウェルスダイナミクスが「まだ分からない」人には、勇誠義礼だけの4本が出る。
インサイドクラス前に触る人がいちばん必要とするところなので、ここは必ず残すこと。

### できていないこと

- **実機（iOS Safari / Android Chrome）での確認。最初にやること**
- **コピーボタンの実動作**。検証に使ったブラウザがクリップボードを許可しない設定だった。
  フォールバックが働いて本文が開き、手で選べる状態になることまでは確認ずみ
- **会員だけがアクセスできる状態**。ホスティング側の設定で、コードの外
- **実在事例33件の分野わけは自動判定**。`tools/make-cases.mjs` の `REAL_GENRE` の正規表現で
  当てているだけなので、おかしなものが混ざる可能性がある。目で見て直すとよい
- 運営への確認事項：勇誠義礼の腕組み対応表の検算（協会の公式noteに記載あり）／
  「16タイプ」の正体（協会の公式は4タイプ）／キックオフで使うのか2週目で使うのか

### 変えていないもの

- 勇誠義礼4タイプの定義文とウェルスダイナミクスの表。出典どおりそのまま
- 単一選択はタップした瞬間に次へ進む（確定ボタンなし）

---

## 配色と書体を変えた理由（§9 の「変える場合は理由を添えること」）

2026-09-14、植田さんの指示で「AIで作った感が強い。TEAM2030らしくカラフルに」。
プロトタイプの明朝＋深緑＋朱鷺色から、次に変えた。

**色は飾りではなく索引にした。** 「気になっていること」10項目に固有の色を割り当て
（`--h0`〜`--h9`、`data/cases.json` の `issues` と同じ並び）、選んだ課題の色が
そのまま結果画面の事例カードの上辺に出る。カードの色は「その事例が、自分の選んだ
どの課題に効くか」で決まる。勇誠義礼の4タイプにも固有色がある（勇＝朱、誠＝青、義＝橙、礼＝桃）。

各色は2段持ち。`--hN` は文字にも使うので白地で読める濃さ、`--hNv` は面を塗る鮮やかな側。
チップの白文字は必ず `--hN` の側に乗せている。課題を増やすときは `style.css` に
`--h10` / `--h10v` を足せばよく、無ければ既定色に落ちる（`app.js` の `hue()`）。

**書体**は見出しを Zen Maru Gothic（丸ゴシック）に、本文は端末のゴシックのまま。
明朝→丸ゴシックが「ポップさ」のいちばん効く一手で、本文を触らないぶん読みやすさは落ちない。

**新しく増えた依存が1つある。** Google Fonts の Zen Maru Gothic を `<link>` で読んでいる。
§9 の「外部CDNなし」からは外れる。フォールバックに Hiragino Maru Gothic ProN 以下の
端末書体を積んであるので、**読めなくても表示は崩れない**（UTAGEの埋め込みで外部読み込みが
止められている場合もここで吸収される）。見出しだけ・1ウェイトだけに絞って重さは抑えたが、
外部読み込みを一切許さない方針なら `index.html` と `build.mjs` の `<link>` 3行を消せば戻る。
